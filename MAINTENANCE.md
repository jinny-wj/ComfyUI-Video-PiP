# 更新与环境保护

本插件不运行 pip/uv/conda，不下载模型，不修改 ComfyUI 核心或其他节点。
使用宿主已有依赖；缺失时停止本节点，不自动修复环境。

更新前保存工作流并备份插件到 custom_nodes 外部，在测试实例验证后再替换。
不要同时保留包含相同节点 ID 的旧版与新版插件目录；关闭 ComfyUI 后替换，再重启。
不要使用“更新所有依赖”作为本插件的更新步骤。
回退时关闭 ComfyUI，恢复备份目录即可；如果另行升级了宿主依赖，代码回退不能撤销依赖升级。

开发期曾将两个节点放在 comfyui-subtitle-safe-zone 同一个包中。
如果装过该合并原型，迁移时先将它移出 custom_nodes，再安装这两个独立仓库，避免重复注册。
两个独立仓库可以同时安装。
